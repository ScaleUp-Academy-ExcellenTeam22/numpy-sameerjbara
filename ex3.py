import numpy as np


def get_row_col(matrix: np.ndarray) -> tuple:
    """
    The function find the number of rows and columns of a given matrix.
    @param matrix: The given matrix.
    @return: tuple contains the row and col of the matrix.
    """
    return matrix.shape
